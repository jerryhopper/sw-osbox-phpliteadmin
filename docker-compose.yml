version: "2.1"
services:
  sqlitebrowser:
    image: richarvey/nginx-php-fpm
    container_name: sw-osbox-phpliteadmin
    volumes:
      - ./code:/var/www/html
      - /etc/osbox:/db
    ports:
      - 2015:80
    restart: unless-stopped
