version: "2.1"
services:
  sqlitebrowser:
    image: webdevops/php-nginx:7.0
    container_name: phpliteadmin
    volumes:
      - ./code:/application/code/
      - /etc/osbox:/db
    ports:
      - 2015:9501
    restart: unless-stopped
